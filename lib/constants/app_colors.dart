import 'package:flutter/material.dart';

class AppColors {
  // Primary colors inspired by Pinterest e-commerce design
  static const Color primary = Color(0xFFE60023); // Pinterest red
  static const Color primaryLight = Color(0xFFFF4757);
  static const Color primaryDark = Color(0xFFB71C1C);

  // Secondary colors
  static const Color secondary = Color(0xFFFF6B35); // Orange accent
  static const Color secondaryLight = Color(0xFFFF8A50);
  static const Color secondaryDark = Color(0xFFE55100);

  // Neutral colors
  static const Color background = Color(0xFFF8F9FA);
  static const Color surface = Color(0xFFFFFFFF);
  static const Color surfaceVariant = Color(0xFFF5F5F5);

  // Text colors
  static const Color textPrimary = Color(0xFF212121);
  static const Color textSecondary = Color(0xFF757575);
  static const Color textHint = Color(0xFFBDBDBD);

  // Status colors
  static const Color success = Color(0xFF4CAF50);
  static const Color warning = Color(0xFFFF9800);
  static const Color error = Color(0xFFF44336);

  // Border and divider colors
  static const Color border = Color(0xFFE0E0E0);
  static const Color divider = Color(0xFFEEEEEE);

  // Shadow color
  static const Color shadow = Color(0x1A000000);
}
